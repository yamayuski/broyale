{
  "tasks": {
    "dev": "deno task dev:proxy & deno task dev:vite",
    "dev:proxy": "deno -A npm:local-ssl-proxy --cert ../certs/fullchain.pem --key ../certs/privkey.pem --source 5172 --target 5173",
    "dev:vite": "deno -A npm:vite"
  }
}
